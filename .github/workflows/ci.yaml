on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-20.04
    name: dummy
    steps:
      - uses: actions/checkout@v2
      - name: mgns
        run: |
          echo hello
          echo world

